---
import particles from "../assets/backgrounds/particles.jpeg";

interface Props {
  animate?: boolean;
  title?: string;
  subtitle?: string;
  firstOfGroup?: boolean;
}

const { animate = true, title, subtitle, firstOfGroup } = Astro.props;
---

{
  firstOfGroup && (
    <section
      data-background-image={particles}
      data-auto-animate={animate || firstOfGroup}
    >
      <h1 class="centered">{title}</h1>
    </section>
  )
}
<section
  data-background-image={particles}
  data-auto-animate={animate || firstOfGroup}
>
  <div class="content">
    <h1>{title}</h1>
    <p>{subtitle}</p>
    <slot />
  </div>
</section>

<style lang="scss">
  section {
    height: 100%;
  }

  .content {
    display: grid;
    grid-template: auto auto minmax(0, 1fr) / 100%;
    height: 100%;
    padding-block: 30px;
  }

  h1 {
    font-size: 3rem;
    margin: 0;

    &.centered {
      font-size: 4rem;
      position: absolute;
      inset: 0;
      height: 1em;
      margin: auto;
      line-height: 1.2;
    }
  }

  p {
    font-size: 1.5rem;
    margin: 0.25em auto 1em;
  }
</style>
